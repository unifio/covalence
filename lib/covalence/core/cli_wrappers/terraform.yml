---
version: Terraform v0.7.2
commands:
  apply:
  destroy:
  fmt:
  get:
  graph:
  #init: #too messy
  #output: #handle manually (required arg)
  plan:
  push:
  refresh:
  remote:
    config:
    pull:
    push:
  show:
  #taint: #handle manually (required arg)
  #untaint: #handle manually (required arg)
  validate:
  version:
  #state: todo
    #list:
    #mv:
    #rm:
    #show:
