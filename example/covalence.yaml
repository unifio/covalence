---
  :backends:
    - yaml

  :logger: noop

  :merge_behavior: 'deeper'

  :hierarchy:
    - "environments/%{environment}"
    - "stacks/%{stack}"
    - "globals"
    - "environments"

  :yaml:
    :datadir: data
