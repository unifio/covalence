---
:backends:
  - yaml

:logger: noop

:merge_behavior: 'deeper'

:hierarchy:
  - "stacks/%{environment}-%{stack}"
  - "envs/%{environment}"
  - 'envs'
  - 'global'

:yaml:
   :datadir: spec/fixtures/data
